/target
*.rlib
# crates/app-dylib is its own workspace root, so a cargo call inside it writes these.
/crates/app-dylib/Cargo.lock
/crates/app-dylib/target/

# Local-only development coordination artifacts
SESSION_HANDOFF.md
.claude/settings.local.json
.superpowers/
.pnpm-store/

# Byte-compiled / optimized / DLL files
__pycache__/
.pytest_cache/
*.py[cod]

# C extensions
*.so

# Distribution / packaging
.Python
.venv/
# `just chronos-up` — the Chronos-2 server's python env (weights + torch).
.venv-chronos/
env/
bin/
build/
develop-eggs/
eggs/
lib/
lib64/
parts/
sdist/
var/
include/
man/
venv/
*.egg-info/
.installed.cfg
*.egg

# Installer logs
pip-log.txt
pip-delete-this-directory.txt
pip-selfcheck.json

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.cache
nosetests.xml
coverage.xml
*.profraw
lcov.info

# Translations
*.mo

# Mr Developer
.mr.developer.cfg
.project
.pydevproject

# Rope
.ropeproject

# Django stuff:
*.log
*.pot

.DS_Store

# Sphinx documentation
docs/_build/

# PyCharm
.idea/

# VSCode
.vscode/launch.json

# Cursor
.history/

# Poetry
oxy/**/poetry.lock

# Oxy
.db-*/
.fastembed_cache/
# `just up` pids, logs and state.json
.oxy-dev/
# Dev env files may carry OXY_OWNER/OXY_GLOBAL_ADMINS allow-lists — keep off the public mirror.
# The two dev-stack files below are tracked (private repo only); copybara push_exclude in
# opensource.yaml keeps them off the public oxy-hq/oxygen mirror.
.env
.env.*
!.env.example
!*.example
!.env.airhouse
!.env.clickhouse

**/node_modules
.turbo/*
# Credential files — never commit. Includes the demo_project /
# examples cloud-warehouse sample keys customers may drop in locally.
*.key
examples/output
examples/.checkpoint/
examples/.lancedb/
examples/.cubestore/
examples/.semantics-test-*
examples/.test-semantics-output
examples/.databases/

# App result caches — regenerated per-run, contain absolute local paths
examples/data/*/*.app.data.yml
examples/data/*/*.parquet

# this folder is used to store the artifacts generated by the github pipeline
artifacts/

.qodo
examples/omni
examples/omni_bigquery.json
examples/.oxy_state/
.copilot-codeGeneration-instructions.md
.playwright-mcp/
# Screenshots land wherever the MCP tool is told to put them, and it defaults
# to the repo root. Three named files did not cover that, and one was committed.
/*.png
examples/test-file-for-e2e.txt
examples/agents/test-agent-e2e.agent.yml
docs/superpowers/
.claude/worktrees/
# Added by code-review-graph
.code-review-graph/

# Playwright MCP scratch output (snapshots, screenshots, console logs)
.playwright-mcp/
