{
  "$schema": "https://v2-10-12.turborepo.dev/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", "target/**"],
      "env": ["VITE_SENTRY_*"],
      "passThroughEnv": ["SENTRY_AUTH_TOKEN", "SENTRY_ORG"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "lint:ts": {
      "dependsOn": ["^lint:ts"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
