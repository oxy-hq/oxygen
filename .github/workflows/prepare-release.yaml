name: Prepare Release
on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "[0-9]+.[0-9]+.x"
    paths-ignore:
      - "docs/**"

jobs:
  # Under the deploy train (internal-docs/deploy-pipeline.md) a release is a
  # *publish*, not a deploy, so a push to main no longer OPENS a release PR — that
  # would re-open one after every merge, the ceremony the train removed. It is
  # opened weekly by `publish-release.yaml`, by hand (`just release-trigger`), or by
  # a push to an `X.Y.x` hotfix branch.
  #
  # But a push to main still REFRESHES one that is already open. Release PRs sit
  # for hours to days before someone merges them, and the tag lands on the merge,
  # so everything merged meanwhile is in the release. The PR body is what
  # `content-changelog.yaml` drafts the public changelog from: frozen at Thursday
  # 10:00, it would silently drop every PR merged while the release waited.
  release-pr-open:
    name: Is a release PR open?
    if: >-
      github.event_name == 'push' && github.ref == 'refs/heads/main' &&
      !startsWith(github.event.head_commit.message, 'chore: release')
    runs-on: ${{ github.repository == 'oxy-hq/oxygen-internal' && 'ubuntu-slim' ||
      'ubuntu-latest' }}
    timeout-minutes: 5
    permissions:
      pull-requests: read
    outputs:
      open: ${{ steps.check.outputs.open }}
    steps:
      - id: check
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          N=$(gh pr list -R "$GITHUB_REPOSITORY" --label autorelease --base main \
            --state open --json number --jq 'length')
          if [[ "$N" -gt 0 ]]; then OPEN=true; else OPEN=false; fi
          echo "open=${OPEN}" >> "$GITHUB_OUTPUT"
          echo "::notice::open release PRs on main: ${N} — $([[ "$OPEN" == true ]] && echo refreshing || echo 'none; not opening one')"

  # Create or update the release PR with version bump and changelog.
  # Skipped when a release PR is being merged (commit message starts with "chore: release").
  release-pr:
    name: Create/Update Release PR
    needs: [ release-pr-open ]
    # `!cancelled()` because the gate is SKIPPED on every trigger but a main push,
    # and a skipped `needs` skips this job too without it. A gate that failed
    # leaves `open` empty, which reads as "do not open one" — the safe direction.
    if: >-
      !cancelled() &&
      !startsWith(github.event.head_commit.message, 'chore: release') &&
      (!(github.event_name == 'push' && github.ref == 'refs/heads/main') ||
        needs.release-pr-open.outputs.open == 'true')
    runs-on: ${{ github.repository == 'oxy-hq/oxygen-internal' && 'ubuntu-slim' ||
      'ubuntu-latest' }}
    timeout-minutes: 20
    concurrency:
      group: release-pr-${{ github.ref }}
      cancel-in-progress: false
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        name: Create GitHub App Token
        id: app-token
        with:
          client-id: ${{ vars.ARGO_APP_CLIENT_ID }}
          private-key: ${{ secrets.ARGO_APP_PRIVATE_KEY }}
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
          token: ${{ steps.app-token.outputs.token }}
      - name: Install git-cliff
        uses: taiki-e/install-action@3f74d7c16a4242f1c95561e98edc25d36adb4375 # v2
        with:
          tool: git-cliff
      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
      - name: Bump version in Cargo.toml
        id: bump
        run: |
          NEW_VERSION=$(uv run scripts/release/bump-version.py)
          echo "version=$NEW_VERSION" >> "$GITHUB_OUTPUT"
      - name: Generate full CHANGELOG.md
        run: git cliff --tag ${{ steps.bump.outputs.version }} -o CHANGELOG.md
      - name: Generate release PR body
        run: git cliff --tag ${{ steps.bump.outputs.version }} --unreleased -o
          /tmp/release-notes.md
      - name: Create or update release PR
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8
        with:
          token: ${{ steps.app-token.outputs.token }}
          commit-message: "chore: release ${{ steps.bump.outputs.version }}"
          branch: release/${{ steps.bump.outputs.version }}
          title: "chore: release ${{ steps.bump.outputs.version }}"
          body-path: /tmp/release-notes.md
          labels: autorelease

  # Create git tag and GitHub release when a release PR is merged.
  # Triggered when the merge commit message starts with "chore: release".
  release:
    name: Tag and Release
    if: "startsWith(github.event.head_commit.message, 'chore: release')"
    runs-on: ${{ github.repository == 'oxy-hq/oxygen-internal' && 'ubuntu-slim' ||
      'ubuntu-latest' }}
    timeout-minutes: 20
    concurrency:
      group: release-${{ github.ref }}
      cancel-in-progress: false
    permissions:
      contents: write
      pull-requests: read
    outputs:
      releases_created: "true"
      release_version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        name: Create GitHub App Token
        id: app-token
        with:
          client-id: ${{ vars.ARGO_APP_CLIENT_ID }}
          private-key: ${{ secrets.ARGO_APP_PRIVATE_KEY }}
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
          token: ${{ steps.app-token.outputs.token }}
      - name: Extract version from commit message
        id: version
        run: |
          VERSION=$(echo "${{ github.event.head_commit.message }}" | head -1 | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
      - name: Create and push git tag
        run: |
          git config user.name "${{ steps.app-token.outputs.app-slug }}[bot]"
          git config user.email "$(gh api "/users/${{ steps.app-token.outputs.app-slug }}[bot]" --jq .id)+${{ steps.app-token.outputs.app-slug }}[bot]@users.noreply.github.com"
          git tag ${{ steps.version.outputs.version }}
          git push origin ${{ steps.version.outputs.version }}
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
      - name: Install git-cliff
        uses: taiki-e/install-action@3f74d7c16a4242f1c95561e98edc25d36adb4375 # v2
        with:
          tool: git-cliff
      - name: Generate release notes
        run: git cliff --current -o /tmp/release-notes.md
      - name: Create GitHub Release
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3
        with:
          token: ${{ steps.app-token.outputs.token }}
          tag_name: ${{ steps.version.outputs.version }}
          name: ${{ steps.version.outputs.version }}
          body_path: /tmp/release-notes.md

  # Tag the public oxy-hq/oxygen repo when a release is created.
  release-public:
    needs: [ release ]
    if: needs.release.outputs.releases_created == 'true'
    runs-on: ${{ github.repository == 'oxy-hq/oxygen-internal' && 'ubuntu-slim' ||
      'ubuntu-latest' }}
    timeout-minutes: 20
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        name: Create GitHub App Token
        id: app-token
        with:
          client-id: ${{ vars.ARGO_APP_CLIENT_ID }}
          private-key: ${{ secrets.ARGO_APP_PRIVATE_KEY }}
          repositories: |
            oxygen-internal
            oxygen
      - name: Get GitHub App User ID
        id: get-user-id
        run: echo "user-id=$(gh api "/users/${{ steps.app-token.outputs.app-slug
          }}[bot]" --jq .id)" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
      - name: Wait for publishing oss code to succeed
        uses: lewagon/wait-on-check-action@369769072fe522a3a8a85c03c96af1e5242a1994 # v1.9.1
        with:
          ref: ${{ github.ref }}
          check-name: "move-code"
          repo-token: ${{ steps.app-token.outputs.token }}
          wait-interval: 20
      - name: Checkout oss repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
          repository: oxy-hq/oxygen
          ref: ${{ github.ref_name }}
          token: ${{ steps.app-token.outputs.token }}
      - name: Tag the release
        run: |
          git config --global user.name "${{ steps.app-token.outputs.app-slug }}[bot]"
          git config --global user.email "${{ steps.get-user-id.outputs.user-id }}+${{ steps.app-token.outputs.app-slug }}[bot]@users.noreply.github.com"
          git tag ${{ needs.release.outputs.release_version }}
          git push origin ${{ needs.release.outputs.release_version }}
